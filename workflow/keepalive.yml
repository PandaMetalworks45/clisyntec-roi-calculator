name: Keep Streamlit Alive
on:
  schedule:
    - cron: '0 */6 * * *' # Runs every 6 hours
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://clisyntec-roi-calculator.streamlit.app
